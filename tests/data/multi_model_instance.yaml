engine: bluesky
frontend: pyqt
models:
  First mock detector:
    plugin_name: mocks
    plugin_id: mock_detector
    sensor_size: [10, 10]
  Second mock detector:
    plugin_name: mocks
    plugin_id: mock_detector
    sensor_size: [10, 10]
    exposure_egu: s
  Single axis motor:
    plugin_name: mocks
    plugin_id: mock_motor
    axes:
      - X
  Double axis motor:
    plugin_name: mocks
    plugin_id: mock_motor
    axes:
      - X
      - Y
    step_egu: mm
